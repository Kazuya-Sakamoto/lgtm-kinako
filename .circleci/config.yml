version: 2.1
executors:
  default:
    docker:
      - image: circleci/node:18.17.1
        environment:
          TZ: Asia/Tokyo
          working_directory: ~/repo/src

commands:
  restore_yarn:
    steps:
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "src/yarn.lock" }}
            - v1-dependencies-

  save_yarn:
    steps:
      - save_cache:
          paths:
            - src/node_modules
          key: v1-dependencies-{{ checksum "src/yarn.lock" }}

  restore_node:
    steps:
      - restore_cache:
          key: node-v2-{{ checksum "src/yarn.lock" }}

  save_node:
    steps:
      - save_cache:
          key: node-v2-{{ checksum "src/yarn.lock" }}
          paths:
            - src/node_modules

jobs:
  node:
    executor:
      name: default
    steps:
      - checkout
      - restore_yarn
      - save_yarn
      - run: cd src && yarn install
      - restore_node
      - save_node

workflows:
  version: 2
  build_and_test:
    jobs:
      - node:
          filters:
            tags:
              only: /.*/
            branches:
              only: /.*/
